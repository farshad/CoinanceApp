import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    logo: {
        height: 90,
        width: 90,
        margin: 0
    },
    sendCodeBottom: {
        marginBottom: 10,
    },
    mainGrid: {
        alignItems: 'center',
        justifyContent: 'center'
    },
    getCodeTitle: {
        fontSize: 20, 
        fontWeight: 'bold'
    },
    mobileExample: {
        padding: 5,
        color: "green",
        marginBottom: 5
    },
});